unlock = {
	custom_tooltip = {
		fail_text = zone_city_02_prereq
		if = {
			limit = {
				exists = owner
				owner = {
					is_primitive = yes
				}
			}
			always = yes
		}
		else = {
			has_upgraded_capital = yes
		}
	}
}
