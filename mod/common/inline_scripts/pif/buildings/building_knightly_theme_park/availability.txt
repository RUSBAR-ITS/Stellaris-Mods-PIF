# PIF building category: AVAILABILITY
# Vanilla building: building_knightly_theme_park
# Vanilla class: branch
# Vanilla source: buildings/14_branch_office_buildings.txt

planet_limit = @pif_building_knightly_theme_park_limits_planet_limit_1

potential = {
	has_branch_office = yes
	branch_office_owner = {
		has_origin = origin_toxic_knights
		NOT = {
			has_country_flag = quest_abandoned
		}
	}
}

allow = {
	pop_amount >= @pif_building_knightly_theme_park_availability_thresholds_pop_amount_1
}
