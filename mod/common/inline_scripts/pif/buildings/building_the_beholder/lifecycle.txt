# PIF building category: LIFECYCLE
# Vanilla building: building_the_beholder
# Vanilla class: regular
# Vanilla source: buildings/17_paragon_buildings.txt

can_demolish = no

destroy_trigger = {
	OR = {
		NOT = {
			exists = owner
		}
		AND = {
			exists = event_target:beholder_leader
			exists = event_target:beholder_leader.owner
			owner = {
				NOT = {
					is_same_value = event_target:beholder_leader.owner
				}
			}
		}
		AND = {
			NOT = {
				exists = event_target:beholder_leader.owner
			}
			owner = {
				NOT = {
					has_country_flag = beholder_chain_owner
				}
			}
		}
	}
}
