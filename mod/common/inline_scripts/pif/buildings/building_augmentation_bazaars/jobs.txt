# PIF building category: JOBS
# Vanilla building: building_augmentation_bazaars
# Vanilla class: regular
# Vanilla source: buildings/20_machine_age_buildings.txt

triggered_planet_modifier = {
	potential = {
		always = yes
	}
	job_augmentor_add = @pif_building_augmentation_bazaars_jobs_job_augmentor_add_1
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_worker_coop_empire = no
			is_gestalt = no
		}
		OR = {
			NOT = {
				has_planet_flag = toxic_knights_habitat
			}
			NOT = {
				owner = {
					has_origin = origin_toxic_knights
				}
			}
		}
	}
	job_politician_add = @pif_building_augmentation_bazaars_jobs_job_politician_add_2
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_worker_coop_empire = yes
		}
	}
	job_bureaucrat_add = @pif_building_augmentation_bazaars_jobs_job_bureaucrat_add_3
	mult = @pif_building_augmentation_bazaars_jobs_job_bureaucrat_add_4
}
