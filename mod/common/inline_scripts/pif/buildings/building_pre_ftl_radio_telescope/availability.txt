# PIF building category: AVAILABILITY
# Vanilla building: building_pre_ftl_radio_telescope
# Vanilla class: regular
# Vanilla source: buildings/11_primitive_buildings.txt

can_build = no

planet_limit = @pif_building_pre_ftl_radio_telescope_limits_planet_limit_1

potential = {
	exists = owner
	owner = {
		is_primitive = yes
		OR = {
			has_country_flag = atomic_age
			has_country_flag = early_space_age
		}
	}
}
