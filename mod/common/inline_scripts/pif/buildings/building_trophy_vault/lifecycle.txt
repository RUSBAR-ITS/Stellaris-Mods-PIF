# PIF building category: LIFECYCLE
# Vanilla building: building_trophy_vault
# Vanilla class: regular
# Vanilla source: buildings/08_unity_buildings.txt

destroy_trigger = {
	exists = owner
	OR = {
		has_modifier = slave_colony
		owner = {
			NOT = {
				has_valid_civic = civic_machine_servitor
			}
		}
	}
}

convert_to = {
	building_autocurating_vault
	building_citadel_of_faith
	building_alpha_hub
	building_corporate_vault
}
