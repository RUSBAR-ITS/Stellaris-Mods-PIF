# PIF building category: AVAILABILITY
# Vanilla building: building_medical_1
# Vanilla class: regular
# Vanilla source: buildings/07_amenity_buildings.txt

planet_limit = @pif_building_medical_1_limits_planet_limit_1

allow = {
	exists = owner
	NOR = {
		has_modifier = slave_colony
		has_modifier = penal_colony
	}
	owner = {
		is_regular_empire = yes
		is_individual_machine = no
	}
	OR = {
		has_planet_flag = ignore_ai_building_limitations
		owner = {
			is_ai = no
		}
		free_amenities < @pif_building_medical_1_availability_thresholds_free_amenities_1
	}
	NOT = {
		has_building = building_low_tech_capital
	}
}

prerequisites = {
	"tech_basic_health"
}
