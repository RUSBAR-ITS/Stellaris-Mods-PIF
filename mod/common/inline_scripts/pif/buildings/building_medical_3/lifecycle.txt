# PIF building category: LIFECYCLE
# Vanilla building: building_medical_3
# Vanilla class: regular
# Vanilla source: buildings/07_amenity_buildings.txt

destroy_trigger = {
	exists = owner
	OR = {
		owner = {
			is_regular_empire = no
		}
		AND = {
			buildings_stop_pop = yes
			owner = {
				is_ai = yes
			}
		}
	}
}
