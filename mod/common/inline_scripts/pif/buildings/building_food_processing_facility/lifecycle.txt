# PIF building category: LIFECYCLE
# Vanilla building: building_food_processing_facility
# Vanilla class: regular
# Vanilla source: buildings/03_resource_buildings.txt

destroy_trigger = {
	exists = owner
	OR = {
		owner = {
			OR = {
				AND = {
					is_ai = yes
					country_uses_food = no
				}
				is_wilderness_empire = yes
			}
		}
		is_planet_class = pc_machine
		has_modifier = resort_colony
		AND = {
			owner = {
				is_ai = yes
			}
			has_any_farming_district_or_building = no
		}
	}
}

upgrades = {
	building_food_processing_center
}
