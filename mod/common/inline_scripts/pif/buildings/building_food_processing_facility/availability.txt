# PIF building category: AVAILABILITY
# Vanilla building: building_food_processing_facility
# Vanilla class: regular
# Vanilla source: buildings/03_resource_buildings.txt

planet_limit = @pif_building_food_processing_facility_limits_planet_limit_1

potential = {
	exists = owner
	owner = {
		is_wilderness_empire = no
	}
	NOR = {
		is_planet_class = pc_machine
		has_modifier = resort_colony
	}
	OR = {
		owner = {
			is_ai = no
		}
		has_any_farming_district_or_building = yes
	}
}

prerequisites = {
	"tech_food_processing_1"
}

show_tech_unlock_if = {
	is_wilderness_empire = no
}
