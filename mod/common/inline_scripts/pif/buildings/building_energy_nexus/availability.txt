# PIF building category: AVAILABILITY
# Vanilla building: building_energy_nexus
# Vanilla class: regular
# Vanilla source: buildings/03_resource_buildings.txt

can_build = no

potential = {
	NOT = {
		has_modifier = resort_colony
	}
	exists = owner
	owner = {
		is_wilderness_empire = no
	}
	OR = {
		owner = {
			is_ai = no
		}
		has_any_generator_district_or_building = yes
	}
}

allow = {
	has_upgraded_capital = yes
}

prerequisites = {
	tech_power_hub_2
}

show_tech_unlock_if = {
	is_wilderness_empire = no
}
