# PIF building category: AVAILABILITY
# Vanilla building: building_planetary_shield_generator
# Vanilla class: regular
# Vanilla source: buildings/09_army_buildings.txt

planet_limit = @pif_building_planetary_shield_generator_limits_planet_limit_1

potential = {
	exists = owner
	owner = {
		is_wilderness_empire = no
	}
}

allow = {
	has_upgraded_capital = yes
	if = {
		limit = {
			exists = orbital_defence
		}
		orbital_defence = {
			exists = starbase
			starbase = {
				NOR = {
					has_starbase_building = ring_orbital_shield_generator
					is_starbase_building_building = ring_orbital_shield_generator
				}
			}
		}
	}
}

prerequisites = {
	"tech_planetary_shield_generator"
}

show_tech_unlock_if = {
	is_wilderness_empire = no
}
