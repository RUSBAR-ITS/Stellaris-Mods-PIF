# PIF building category: LIFECYCLE
# Vanilla building: building_psionic_offices
# Vanilla class: branch
# Vanilla source: buildings/14_branch_office_buildings.txt

on_built = {
	solar_system = {
		set_star_flag = building_psionic_offices_emitting@root.branch_office_owner
		if = {
			limit = {
				is_psionic_aura_suppressed = no
			}
			natural_spawn_psionic_aura = {
				OWNER = root.branch_office_owner
			}
		}
	}
}

on_destroy = {
	solar_system = {
		remove_star_flag = building_psionic_offices_emitting@root.branch_office_owner
	}
}
