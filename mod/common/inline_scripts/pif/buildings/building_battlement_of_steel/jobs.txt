# PIF building category: JOBS
# Vanilla building: building_battlement_of_steel
# Vanilla class: regular
# Vanilla source: buildings/20_machine_age_buildings.txt

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_regular_empire = yes
		}
	}
	job_bureaucrat_add = @pif_building_battlement_of_steel_jobs_job_bureaucrat_add_1
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_regular_empire = no
		}
	}
	job_coordinator_add = @pif_building_battlement_of_steel_jobs_job_coordinator_add_2
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = no
		}
		NOT = {
			has_zone = {
				zone = zone_habitat_knights
			}
			has_modifier = slave_colony
		}
	}
	job_soldier_add = @pif_building_battlement_of_steel_jobs_job_soldier_add_3
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = no
		}
		NOT = {
			has_zone = {
				zone = zone_habitat_knights
			}
		}
		has_modifier = slave_colony
	}
	job_battle_thrall_add = @pif_building_battlement_of_steel_jobs_job_battle_thrall_add_4
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = no
		}
		has_zone = {
			zone = zone_habitat_knights
		}
		NOT = {
			has_modifier = slave_colony
		}
	}
	job_knight_add = @pif_building_battlement_of_steel_jobs_job_knight_add_5
	mult = @pif_building_battlement_of_steel_jobs_job_knight_add_6
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
		}
	}
	job_warrior_drone_add = @pif_building_battlement_of_steel_jobs_job_warrior_drone_add_7
}
