# PIF building category: AVAILABILITY
# Vanilla building: building_automation_farmer_2
# Vanilla class: regular
# Vanilla source: buildings/01_pop_assembly_buildings.txt

can_build = no

planet_limit = @pif_building_automation_farmer_2_limits_planet_limit_1

potential = {
	owner = {
		is_wilderness_empire = no
		is_tankbound_empire = no
	}
}

allow = {
	hidden_trigger = {
		OR = {
			owner = {
				is_ai = no
			}
			NAND = {
				free_district_slots = @pif_building_automation_farmer_2_planet_state_free_district_slots_1
				free_building_slots <= @pif_building_automation_farmer_2_planet_state_free_building_slots_2
				free_housing <= @pif_building_automation_farmer_2_planet_state_free_housing_3
				free_jobs <= 0
			}
		}
	}
}

prerequisites = {
	tech_construction_templates
}

show_tech_unlock_if = {
	always = no
}
