# PIF building category: AVAILABILITY
# Vanilla building: building_ranger_lodge
# Vanilla class: regular
# Vanilla source: buildings/05_research_buildings.txt

planet_limit = @pif_building_ranger_lodge_limits_planet_limit_1

potential = {
	NOT = {
		has_modifier = slave_colony
	}
	owner = {
		is_regular_empire = yes
		has_valid_civic = civic_environmentalist
	}
	NOR = {
		is_planet_class = pc_city
		is_planet_class = pc_hive
		is_planet_class = pc_machine
		is_planet_class = pc_relic
	}
	is_artificial = no
}

allow = {
	custom_tooltip = {
		fail_text = no_free_district_for_ranger_lodge
		planet = {
			num_free_districts = {
				type = any
				value > 0
			}
		}
	}
}
