# PIF building category: AVAILABILITY
# Vanilla building: building_system_capital
# Vanilla class: capital
# Vanilla source: buildings/00_capital_buildings.txt

can_build = no

potential = {
	exists = owner
	owner = {
		is_regular_empire = yes
		is_fallen_empire = no
	}
	NOR = {
		has_modifier = resort_colony
		has_modifier = slave_colony
	}
}

allow = {
	sapient_pop_amount >= @pif_building_system_capital_availability_thresholds_sapient_pop_amount_1
}

prerequisites = {
	"tech_galactic_administration"
}

show_tech_unlock_if = {
	is_regular_empire = yes
}
