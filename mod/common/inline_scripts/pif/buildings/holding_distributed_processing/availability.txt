# PIF building category: AVAILABILITY
# Vanilla building: holding_distributed_processing
# Vanilla class: holding
# Vanilla source: buildings/15_overlord_holdings.txt

planet_limit = @pif_holding_distributed_processing_limits_planet_limit_1

potential = {
	owner = {
		is_wilderness_empire = no
		overlord = {
			is_machine_empire = yes
			NOT = {
				has_civic = civic_machine_servitor
			}
		}
	}
	has_overlord_dlc = yes
}
