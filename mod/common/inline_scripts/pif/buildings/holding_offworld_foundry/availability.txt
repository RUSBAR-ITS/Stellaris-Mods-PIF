# PIF building category: AVAILABILITY
# Vanilla building: holding_offworld_foundry
# Vanilla class: holding
# Vanilla source: buildings/15_overlord_holdings.txt

planet_limit = @pif_holding_offworld_foundry_limits_planet_limit_1

potential = {
	has_overlord_dlc = yes
	exists = owner
	owner = {
		is_wilderness_empire = no
	}
}

allow = {
	custom_tooltip = {
		fail_text = subject_is_prospectorium
		exists = planet.owner
		planet.owner = {
			is_specialist_subject_type = {
				TYPE = prospectorium
			}
		}
	}
}
