# PIF building category: AVAILABILITY
# Vanilla building: building_ministry_production
# Vanilla class: regular
# Vanilla source: buildings/04_manufacturing_buildings.txt

planet_limit = @pif_building_ministry_production_limits_planet_limit_1

potential = {
	exists = owner
	owner = {
		is_regular_empire = yes
	}
	NOR = {
		has_modifier = resort_colony
		has_modifier = slave_colony
	}
}

allow = {
	has_major_upgraded_capital = yes
}

prerequisites = {
	"tech_global_production_strategy"
}

show_tech_unlock_if = {
	is_regular_empire = yes
}
