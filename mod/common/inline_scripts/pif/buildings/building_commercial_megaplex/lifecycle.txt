# PIF building category: LIFECYCLE
# Vanilla building: building_commercial_megaplex
# Vanilla class: regular
# Vanilla source: buildings/06_trade_buildings.txt

destroy_trigger = {
	exists = owner
	OR = {
		owner = {
			is_regular_empire = no
		}
		has_modifier = slave_colony
		has_modifier = penal_colony
	}
}
