# PIF building category: AVAILABILITY
# Vanilla building: building_capital
# Vanilla class: capital
# Vanilla source: buildings/00_capital_buildings.txt

can_build = no

potential = {
	exists = owner
	owner = {
		is_regular_empire = yes
		is_fallen_empire = no
	}
	NOR = {
		has_modifier = resort_colony
		has_modifier = slave_colony
		uses_habitat_capitals = yes
		is_planet_class = pc_cosmogenesis_world
	}
}

allow = {
	sapient_pop_amount >= @pif_building_capital_availability_thresholds_sapient_pop_amount_1
}

prerequisites = {
	"tech_planetary_government"
}

show_tech_unlock_if = {
	is_regular_empire = yes
}
