# PIF building category: AVAILABILITY
# Vanilla building: building_robot_assembly_plant
# Vanilla class: regular
# Vanilla source: buildings/01_pop_assembly_buildings.txt

planet_limit = @pif_building_robot_assembly_plant_limits_planet_limit_1

potential = {
	exists = owner
	OR = {
		owner = {
			is_ai = no
		}
		NOT = {
			exists = assembling_species
		}
		assembling_species = {
			is_robotic_species = yes
		}
	}
	owner = {
		is_regular_empire = yes
		is_individual_machine = no
		NOT = {
			has_policy_flag = robots_outlawed
		}
		OR = {
			is_ai = no
			NOT = {
				has_ascension_perk = ap_engineered_evolution
			}
			is_materialist = yes
		}
	}
	NOT = {
		has_modifier = resort_colony
	}
}

allow = {
	hidden_trigger = {
		OR = {
			owner = {
				is_ai = no
			}
			NAND = {
				free_district_slots = @pif_building_robot_assembly_plant_planet_state_free_district_slots_1
				free_building_slots <= @pif_building_robot_assembly_plant_planet_state_free_building_slots_2
				free_housing <= @pif_building_robot_assembly_plant_planet_state_free_housing_3
				free_jobs <= 0
			}
		}
	}
}

prerequisites = {
	"tech_robotic_workers"
}
