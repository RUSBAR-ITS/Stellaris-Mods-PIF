# PIF building category: AVAILABILITY
# Vanilla building: holding_splinter_hive
# Vanilla class: holding
# Vanilla source: buildings/15_overlord_holdings.txt

planet_limit = @pif_holding_splinter_hive_limits_planet_limit_1

potential = {
	has_overlord_dlc = yes
	owner = {
		is_wilderness_empire = no
		overlord = {
			is_hive_empire = yes
			is_wilderness_empire = no
		}
	}
}

allow = {
	custom_tooltip = {
		fail_text = no_free_district_for_splinter_hive
		planet = {
			num_free_districts = {
				type = any
				value > 0
			}
		}
	}
	planet = {
		pop_amount >= @pif_holding_splinter_hive_availability_thresholds_pop_amount_1
	}
}
