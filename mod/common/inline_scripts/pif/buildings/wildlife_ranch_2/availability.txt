# PIF building category: AVAILABILITY
# Vanilla building: wildlife_ranch_2
# Vanilla class: special
# Vanilla source: buildings/21_grand_archive_buildings.txt

can_build = no

allow = {
	has_upgraded_capital = yes
}

show_tech_unlock_if = {
	owner = {
		has_origin = origin_primal_calling
		OR = {
			wrangler_is_trophy_hunter_trigger = no
			wrangler_is_animal_handler_trigger = no
			wrangler_is_beastfighter_trigger = no
		}
	}
}

prerequisites = {
	"tech_heritage_site"
}
