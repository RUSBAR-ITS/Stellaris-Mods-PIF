# PIF building category: JOBS
# Vanilla building: primal_arena
# Vanilla class: special
# Vanilla source: buildings/21_grand_archive_buildings.txt

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = no
		}
	}
	job_wrangler_add = @pif_primal_arena_jobs_job_wrangler_add_1
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_gestalt = yes
		}
	}
	job_drone_wrangler_add = @pif_primal_arena_jobs_job_drone_wrangler_add_2
}
