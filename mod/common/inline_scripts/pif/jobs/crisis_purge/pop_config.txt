# PIF job category: POP_CONFIG
# Vanilla job: crisis_purge
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

first_come_first_served = @pif_crisis_purge_job_flags_first_come_first_served_2

ignores_sapience = @pif_crisis_purge_job_flags_ignores_sapience_3

tags = {}

demotion = {
	time = @pif_crisis_purge_demotion_time_1
	effect = {
		pop_amount_change_category = {
			CATEGORY = purge
			AMOUNT = local_pop_amount
		}
	}
}

weight = {
	weight = @pif_crisis_purge_job_weight_weight_1
	modifier = {
		factor = @pif_crisis_purge_job_weight_factor_2
		exists = planet
		exists = planet.controller
		planet.controller = {
			OR = {
				is_country_type = swarm
				is_country_type = ai_empire
			}
		}
	}
	modifier = {
		factor = @pif_crisis_purge_job_weight_factor_3
		exists = planet
		exists = planet.controller
		planet.controller = {
			OR = {
				is_country_type = swarm
				is_country_type = ai_empire
			}
		}
	}
}
