# PIF job category: AVAILABILITY
# Vanilla job: primitive_farmer
# Vanilla class: worker
# Vanilla source: pop_jobs/05_primitive_jobs.txt

is_capped_by_modifier = @pif_primitive_farmer_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_being_purged = no
	is_being_assimilated = no
	is_sapient = yes
}

possible_precalc = can_fill_worker_job

possible = {
	owner = {
		is_primitive = yes
		OR = {
			has_country_flag = steam_age
			has_country_flag = machine_age
			has_country_flag = industrial_age
			has_country_flag = atomic_age
			has_country_flag = early_space_age
		}
	}
}
