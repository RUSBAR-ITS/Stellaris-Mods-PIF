# PIF job category: POP_CONFIG
# Vanilla job: purge_processing_robot
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

first_come_first_served = @pif_purge_processing_robot_job_flags_first_come_first_served_2

demotion = {
	time = @pif_purge_processing_robot_demotion_time_1
	effect = {
		pop_amount_change_category = {
			CATEGORY = purge
			AMOUNT = local_pop_amount
		}
	}
}

tags = {}

ignores_sapience = @pif_purge_processing_robot_job_flags_ignores_sapience_3

weight = {
	weight = @pif_purge_processing_robot_job_weight_weight_1
	modifier = {
		factor = @pif_purge_processing_robot_job_weight_factor_2
		exists = planet
		exists = planet.controller
		OR = {
			has_pop_group_flag = event_purge
			planet.controller = {
				OR = {
					is_country_type = swarm
					is_country_type = ai_empire
				}
			}
		}
	}
	modifier = {
		factor = @pif_purge_processing_robot_job_weight_factor_3
		has_pop_group_flag = event_purge
	}
	modifier = {
		factor = @pif_purge_processing_robot_job_weight_factor_3
		exists = planet
		exists = planet.controller
		planet.controller = {
			OR = {
				is_country_type = swarm
				is_country_type = ai_empire
			}
		}
	}
}
