# PIF job category: ECONOMIC_MODIFIERS
# Vanilla job: slave_overseer
# Vanilla class: worker
# Vanilla source: pop_jobs/00_other_jobs.txt

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			is_oligarchic_authority = yes
			OR = {
				has_country_flag = synth_physical
				has_country_flag = synth_virtual
			}
		}
	}
	planet_jobs_ruler_produces_mult = @pif_slave_overseer_economic_modifiers_planet_jobs_ruler_produces_mult_1
}
