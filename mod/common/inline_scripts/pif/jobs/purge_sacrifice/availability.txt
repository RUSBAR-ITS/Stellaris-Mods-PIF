# PIF job category: AVAILABILITY
# Vanilla job: purge_sacrifice
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

is_capped_by_modifier = @pif_purge_sacrifice_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_being_purged = yes
}

possible = {
	has_purge_type = {
		type = purge_sacrifice
		country = owner
	}
}
