# PIF job category: POP_CONFIG
# Vanilla job: wrangler
# Vanilla class: specialist
# Vanilla source: pop_jobs/14_grand_archive_jobs.txt

tags = {
	research
	unity
	amenities
}

weight = {
	weight = @pif_wrangler_job_weight_weight_1
	mult = value:job_weights_modifier|JOB|wrangler|RESOURCE|unity|FACTOR|0.25|
	mult = value:scripted_modifier_job_weight_mult|MODIFIER|pop_job_amenities_mult|
	modifier = {
		factor = @pif_wrangler_job_weight_factor_2
		has_citizenship_rights = no
		NOT = {
			has_trait = trait_mechanical
		}
	}
	modifier = {
		factor = @pif_wrangler_job_weight_factor_3
		has_trait = trait_robot_domestic_protocols
		OR = {
			is_non_sapient_robot = yes
			is_shackled_robot = yes
		}
	}
	modifier = {
		factor = @pif_wrangler_job_weight_factor_4
		is_domestic_servant = yes
	}
	modifier = {
		factor = @pif_wrangler_job_weight_factor_4
		exists = planet
		planet = {
			free_amenities > 500
		}
	}
	modifier = {
		factor = @pif_wrangler_job_weight_factor_5
		exists = planet
		planet = {
			free_amenities < 200
			has_available_jobs = wrangler
		}
	}
}

auto_trait_prio = {
	trait_intelligent
	trait_artificial_intelligence
	trait_robot_logic_engines
	trait_cyborg_logic_engines
	trait_malleable_research
	trait_adaptive_research
}
