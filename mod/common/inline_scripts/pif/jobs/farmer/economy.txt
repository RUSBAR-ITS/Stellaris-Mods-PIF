# PIF job category: ECONOMY
# Vanilla job: farmer
# Vanilla class: worker
# Vanilla source: pop_jobs/03_worker_jobs.txt

resources = {
	category = planet_farmers
	produces = {
		food = @pif_farmer_job_output_output_food_1
	}
	produces = {
		trigger = {
			exists = owner
			owner = {
				is_robot_empire = yes
			}
		}
		food = @pif_farmer_job_output_output_food_2
	}
	produces = {
		trigger = {
			exists = planet
			planet = {
				farmer_is_angler_trigger = yes
			}
		}
		trade = @pif_farmer_job_output_output_trade_3
	}
	produces = {
		trigger = {
			farmer_is_titan_hunter_trigger = yes
		}
		food = @pif_farmer_job_output_output_food_4
		trade = @pif_farmer_job_output_output_trade_5
	}
	produces = {
		trigger = {
			farmer_is_puddle_technician_trigger = yes
		}
		alloys = @pif_farmer_job_output_output_alloys_6
	}
	produces = {
		trigger = {
			planet = {
				has_planet_flag = has_produce_requisitorium
			}
		}
		food = @pif_farmer_job_output_output_food_7
	}
}

overlord_resources = {
	category = planet_requisitioned_food
	produces = {
		trigger = {
			planet = {
				has_planet_flag = has_produce_requisitorium
			}
		}
		food = @pif_farmer_overlord_output_overlord_output_food_1
	}
}
