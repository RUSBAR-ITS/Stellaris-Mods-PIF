# PIF job category: AVAILABILITY
# Vanilla job: purge_lithoid
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

is_capped_by_modifier = @pif_purge_lithoid_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_being_purged = yes
}

possible = {
	OR = {
		has_citizenship_type = {
			type = citizenship_purge_machine
			country = owner
		}
		has_citizenship_type = {
			type = citizenship_purge
			country = owner
		}
		has_pop_group_flag = event_purge
	}
	NOR = {
		has_purge_type = {
			type = purge_processing
			country = owner
		}
		has_purge_type = {
			type = purge_labor_camps
			country = owner
		}
		has_purge_type = {
			type = purge_matrix
			country = owner
		}
	}
	is_lithoid = yes
}
