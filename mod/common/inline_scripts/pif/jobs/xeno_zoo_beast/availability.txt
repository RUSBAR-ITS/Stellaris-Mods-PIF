# PIF job category: AVAILABILITY
# Vanilla job: xeno_zoo_beast
# Vanilla class: special_other
# Vanilla source: pop_jobs/05_primitive_jobs.txt

is_capped_by_modifier = @pif_xeno_zoo_beast_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	is_sapient = no
}

possible = {
	planet = {
		exists = owner
		owner = {
			has_policy_flag = pre_sapients_protect
			has_valid_civic = civic_warrior_culture
		}
		has_active_building = building_xeno_zoo
	}
	is_lithoid = no
	NOT = {
		has_trait = trait_nascent_stage
	}
}
