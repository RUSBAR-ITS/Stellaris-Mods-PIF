# PIF job category: AVAILABILITY
# Vanilla job: civilian
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

is_capped_by_modifier = @pif_civilian_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_enslaved = no
	is_being_purged = no
	is_being_assimilated = no
	is_sapient = yes
}

possible = {
	hidden_trigger = {
		exists = owner
	}
	owner = {
		is_gestalt = no
	}
	OR = {
		NOT = {
			owner = {
				has_active_tradition = tr_virtuality_finish
			}
		}
		has_virtual_species_trait = no
	}
	planet = {
		NOT = {
			is_planet_class = pc_cosmogenesis_world
		}
	}
}
