# PIF job category: AVAILABILITY
# Vanilla job: ratling_scavenger
# Vanilla class: worker
# Vanilla source: pop_jobs/06_event_jobs.txt

is_capped_by_modifier = @pif_ratling_scavenger_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_sapient = yes
}

possible = {
	hidden_trigger = {
		exists = owner
	}
	owner = {
		is_country_type = ratlings
	}
}
