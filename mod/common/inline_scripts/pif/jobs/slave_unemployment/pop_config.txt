# PIF job category: POP_CONFIG
# Vanilla job: slave_unemployment
# Vanilla class: special_other
# Vanilla source: pop_jobs/15_unemployment.txt

count_as_available_for_ai = @pif_slave_unemployment_job_flags_count_as_available_for_ai_2

can_set_priority = @pif_slave_unemployment_job_flags_can_set_priority_3

contributes_to_diplo_weight = @pif_slave_unemployment_job_flags_contributes_to_diplo_weight_4

allow_only_same_rank_pops = @pif_slave_unemployment_job_flags_allow_only_same_rank_pops_5

demotion = {
	time = @pif_slave_unemployment_demotion_time_1
	effect = {
		if = {
			limit = {
				NOT = {
					is_pop_category = slave
				}
			}
			pop_amount_change_category = {
				CATEGORY = slave
				AMOUNT = local_pop_amount
			}
		}
	}
}

weight = {
	weight = @pif_slave_unemployment_job_weight_weight_1
}
