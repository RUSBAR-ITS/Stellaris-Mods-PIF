# PIF job category: AVAILABILITY
# Vanilla job: heart_processing
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

is_capped_by_modifier = @pif_heart_processing_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_being_purged = no
	is_being_assimilated = no
}

possible = {
	has_trait = trait_wilderness
	NOT = {
		is_pop_category = wilderness_heart_strata
	}
}
