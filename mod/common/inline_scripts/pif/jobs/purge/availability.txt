# PIF job category: AVAILABILITY
# Vanilla job: purge
# Vanilla class: special_other
# Vanilla source: pop_jobs/00_other_jobs.txt

is_capped_by_modifier = @pif_purge_job_flags_is_capped_by_modifier_1

possible_pre_triggers = {
	has_owner = yes
	is_being_purged = yes
	is_robotic = no
}

possible = {
	OR = {
		NOR = {
			has_purge_type = {
				country = owner
				type = purge_processing
			}
			has_purge_type = {
				country = owner
				type = purge_labor_camps
			}
			has_purge_type = {
				country = owner
				type = purge_matrix
			}
		}
		has_pop_group_flag = event_purge
	}
	is_lithoid = no
}
