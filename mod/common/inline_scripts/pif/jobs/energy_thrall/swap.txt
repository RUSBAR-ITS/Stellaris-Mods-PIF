# PIF job category: SWAP
# Vanilla job: energy_thrall
# Vanilla class: worker
# Vanilla source: pop_jobs/16_shroud_jobs.txt

swappable_data = {
	default = {
		condition_string = WORKER_JOB_TRIGGER
		building_icon = building_lifecrypt_1
	}
}
