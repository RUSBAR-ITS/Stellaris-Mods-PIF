# PIF zone category: AVAILABILITY
# Vanilla zone: zone_resort_spiritual_retreat
# Vanilla source: zones/00_zones.txt

potential = {
	exists = owner
	owner = {
		is_wilderness_empire = no
		is_spiritualist = yes
		NOT = {
			has_zone = {
				zone = zone_resort_spiritual_retreat
			}
		}
	}
}

planet_limit = {
	base = @pif_zone_resort_spiritual_retreat_zone_limits_planet_limit_planet_limit_base_base_1
}

unlock = {
	has_modifier = resort_colony
}
