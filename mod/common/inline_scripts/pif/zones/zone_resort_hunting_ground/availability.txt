# PIF zone category: AVAILABILITY
# Vanilla zone: zone_resort_hunting_ground
# Vanilla source: zones/00_zones.txt

potential = {
	exists = owner
	owner = {
		is_wilderness_empire = no
		is_xenophile = no
		NOT = {
			has_zone = {
				zone = zone_resort_hunting_ground
			}
		}
	}
}

planet_limit = {
	base = @pif_zone_resort_hunting_ground_zone_limits_planet_limit_planet_limit_base_base_1
}

unlock = {
	has_modifier = resort_colony
}
