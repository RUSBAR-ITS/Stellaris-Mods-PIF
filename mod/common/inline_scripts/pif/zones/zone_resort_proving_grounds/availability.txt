# PIF zone category: AVAILABILITY
# Vanilla zone: zone_resort_proving_grounds
# Vanilla source: zones/00_zones.txt

potential = {
	exists = owner
	owner = {
		is_wilderness_empire = no
		OR = {
			has_valid_civic = civic_warrior_culture
			is_militarist = yes
		}
		NOT = {
			has_zone = {
				zone = zone_resort_proving_grounds
			}
		}
	}
}

planet_limit = {
	base = @pif_zone_resort_proving_grounds_zone_limits_planet_limit_planet_limit_base_base_1
}

unlock = {
	has_modifier = resort_colony
}
