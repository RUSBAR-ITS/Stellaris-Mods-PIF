# PIF zone category: AVAILABILITY
# Vanilla zone: zone_resort_zoo
# Vanilla source: zones/00_zones.txt

potential = {
	exists = owner
	owner = {
		has_technology = tech_alien_life_studies
		is_wilderness_empire = no
	}
	has_modifier = resort_colony
	NOT = {
		has_zone = {
			zone = zone_resort_zoo
		}
	}
}

planet_limit = {
	base = @pif_zone_resort_zoo_zone_limits_planet_limit_planet_limit_base_base_1
}

unlock = {
	has_modifier = resort_colony
}
